sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t Dichtrich/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true Dichtrich/docker-react npm run test -- --coverage
